export interface Province {
    code: string;
    name: string;
    codeName: string;
}

export interface District {
    code: string;
    name: string;
    codeName: string;
}

export interface Ward {
    code: string;
    name: string;
    codeName: string;
}